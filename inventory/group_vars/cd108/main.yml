# Particularities for lab cd108. Override any all.yml var here, or add
# lab-specific packages/config. Empty = inherits the common defaults.
#
# NOTE: because this `cd108/` directory exists, a sibling `cd108.yml` file would
# be IGNORED by Ansible (directory shadows file). Keep all cd108 group vars in
# here. Secrets (e.g. winvm_rdp_password) live in the encrypted vault.yml.
